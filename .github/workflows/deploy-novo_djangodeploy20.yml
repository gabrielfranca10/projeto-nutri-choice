name: Build and deploy Python app to Azure Web App - djangodeploy20

on:
  push:
    branches:
      - deploy-novo
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          npm ci

      - name: Run local server and Cypress tests
        run: |
          python manage.py migrate
          python manage.py runserver & 
          sleep 5
          npx cypress run

      - name: Zip artifact for deployment
        run: zip release.zip ./* -r

      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            release.zip
            !venv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Unzip artifact for deployment
        run: unzip release.zip

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_7EDAF2250DD3433BB6E54350963FF2EE }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_F920071D37A8474983C5C0BF35E1EB9F }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_28953D58BA0441FD9040119C19802D1B }}

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'djangodeploy20'
          slot-name: 'Production'

      - name: Cleaning testing files
        run: |
          rm -f db.sqlite3
          rm -f .env
          rm -f cypress.config.js
          rm -f package-lock.json
          rm -f package.json
          rm -rf cypress
          rm -rf node_modules

      - name: Zip artifact for deployment
        run: |
          zip release.zip ./* -r
